name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Frontend gates + build the embedded UI. web/dist is uploaded as an artifact and
  # consumed by the Go jobs (the binary embeds it via go:embed), so CI is correct
  # whether or not web/dist is committed.
  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci --prefix web
      - run: make web-lint
      - run: make web-test
      - run: ./web/node_modules/.bin/playwright install --with-deps chromium
      - run: make web-e2e
      - run: make web
      - uses: actions/upload-artifact@v4
        with:
          name: web-dist
          path: web/dist
          retention-days: 1

  lint:
    needs: web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v4
        with: { name: web-dist, path: web/dist }
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          # The module cache holds only golangci-lint, keyed by its pin in the Makefile.
          cache-dependency-path: Makefile
      - run: make lint

  test:
    needs: web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v4
        with: { name: web-dist, path: web/dist }
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - run: make test

  # Real-engine drills in containers (build tag: integration). Needs Docker plus
  # the engine binaries — without them the borg/restic tests skip silently and
  # the gate stops gating.
  integration:
    needs: web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v4
        with: { name: web-dist, path: web/dist }
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      # python3-llfuse is borg's FUSE binding (a Recommends, so it must be
      # named under --no-install-recommends) and fuse3 provides fusermount3 —
      # without them the restore.mode: mount tests error instead of mounting.
      - run: sudo apt-get update && sudo apt-get install -y --no-install-recommends borgbackup restic python3-llfuse fuse3
      - run: make test-integration

  # The sabotage kit (build tag: sabotage): every fixture must be flagged. Engine
  # fixtures need Docker + borg + restic, so install the engines here too.
  sabotage:
    needs: web
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/download-artifact@v4
        with: { name: web-dist, path: web/dist }
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - run: sudo apt-get update && sudo apt-get install -y --no-install-recommends borgbackup restic python3-llfuse fuse3
      - run: make test-sabotage
