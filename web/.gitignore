node_modules
*.local
.DS_Store

# dist/ is intentionally NOT ignored — it's embedded via go:embed, so committing
# it keeps `go build` / `go install` / Go CI working without a Node step.

# Playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache
